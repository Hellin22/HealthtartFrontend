import SignupPage from "@/views/SignupPage.vue";

const SignupRoutes = [
    {
        path: "/users/signup",
        component: SignupPage,
        props: true,
        
    },

];

export default SignupRoutes;